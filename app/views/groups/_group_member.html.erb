<tr id="group_member_<%= group_member.id %>">
	<td><%= group_member.member.fullname %></td>
	<td><%= link_to 'Eliminar', group_remove_member_path(group_member.group, group_member.member), method: :delete, remote: true, data: { confirm: '¿Esta seguro de querer eliminar este miembro?' } %></td>
</tr>